numpy>=1.21.0
opencv-python>=4.5.0
pyyaml>=6.0
tqdm>=4.62.0
requests>=2.26.0

# Inference backends (install whichever applies to your environment)
onnxruntime-gpu>=1.12.0  # Use onnxruntime (CPU only) if no CUDA
# onnxruntime>=1.12.0

# PyTorch backend + model export (required for source=torchvision/huggingface/smp)
# torch>=1.11.0
# torchvision>=0.12.0

# SegFormer and other HuggingFace segmentation models (source=huggingface)
# transformers>=4.26.0

# Classic segmentation models — UNet, FPN, DeepLabV3+ (source=smp)
# segmentation-models-pytorch>=0.3.0

# TensorRT Python bindings are provided by JetPack on Jetson Nano
# Install: sudo apt-get install python3-libnvinfer python3-libnvinfer-dev
# pycuda>=2021.1  # Install: pip install pycuda

# Model download helpers
huggingface_hub>=0.12.0

# YOLO support — YOLOv8 and YOLO11 (source=ultralytics)
# ultralytics>=8.3.0

# RF-DETR — Roboflow Detection Transformer (source=rfdetr)
# rfdetr>=1.0.0

# SuperPoint + LightGlue feature matching (source=lightglue)
# pip install git+https://github.com/cvg/LightGlue.git
# (plain 'pip install lightglue' may lack the onnx export submodule)

# cuVSLAM — NVIDIA Isaac ROS Visual SLAM (backend=cuvslam)
# System-installed via: sudo apt install ros-humble-isaac-ros-visual-slam
# or Isaac ROS container: nvcr.io/nvidia/isaac/ros:aarch64-visual_slam

# Results
pandas>=1.3.0
